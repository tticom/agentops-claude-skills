[pytest]
# tests/ holds repository-level checks; skills/*/scripts/ holds each skill's own
# bundled tests, which travel with the skill.
testpaths = tests skills
pythonpath = scripts
python_files = test_*.py
